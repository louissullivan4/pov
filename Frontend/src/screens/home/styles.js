import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    container: {
        flex:1,
        alignItems: 'center',
    },
    title: {
        flex:1,
        fontSize: 40,
        marginTop: 40,
    },
})
export default styles;